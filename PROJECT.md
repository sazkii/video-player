# PROJECT: 在线视频播放器

## 1. 项目概述

构建一个现代化的在线视频播放网页应用，支持本地视频文件上传播放和在线视频流播放。界面简洁美观，交互流畅，支持常见播放控制功能。

**目标用户**: 个人用户，用于播放本地或在线视频内容
**核心价值**: 零广告、纯净体验、开箱即用的视频播放器

## 2. 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | React 18 + TypeScript | 组件化开发 |
| 样式 | Tailwind CSS | 快速构建响应式UI |
| 构建 | Vite | 快速开发服务器 + 构建 |
| 视频 | HTML5 Video API + HLS.js | 原生播放 + HLS流支持 |
| 状态 | React Hooks (useState/useRef) | 轻量状态管理 |

**不要使用**: Redux、Next.js、任何后端框架
**不需要**: 用户登录、数据库、后端服务

## 3. 功能需求

### P0 — 核心功能（必须实现）
- [ ] 视频播放/暂停控制
- [ ] 进度条拖拽跳转
- [ ] 音量控制（滑块 + 静音切换）
- [ ] 全屏播放
- [ ] 播放速度调节（0.5x / 1x / 1.25x / 1.5x / 2x）
- [ ] 本地文件拖拽上传播放
- [ ] 在线视频URL输入播放

### P1 — 增强功能（应该实现）
- [ ] 画中画（PiP）模式
- [ ] 键盘快捷键（空格=播放/暂停, ←→=快退/快进, F=全屏, M=静音）
- [ ] 倍速记忆（记住用户上次选择的播放速度）
- [ ] 视频缩略图预览（hover进度条时显示）
- [ ] 自定义主题色（暗色/亮色切换）

### P2 — 锦上添花（可以实现）
- [ ] 播放列表（支持多视频连续播放）
- [ ] 字幕加载（支持 .srt 文件导入）
- [ ] 画质切换（当视频有多个分辨率时）
- [ ] 网络视频URL历史记录（localStorage）

## 4. 页面结构

```
┌─────────────────────────────────────────┐
│  Header: "Video Player" + 主题切换      │
├─────────────────────────────────────────┤
│                                         │
│         视频播放区域 (16:9)              │
│         ┌───────────────────┐           │
│         │                   │           │
│         │    Video Player   │           │
│         │                   │           │
│         └───────────────────┘           │
│                                         │
├─────────────────────────────────────────┤
│  控制栏: 播放 | 进度条 | 时间 | 音量 | 速度 | 全屏 │
├─────────────────────────────────────────┤
│  输入区域:                              │
│  [拖拽区域 或 在线URL输入框]             │
│                                         │
│  播放列表（如果有多个视频）              │
└─────────────────────────────────────────┘
```

## 5. 设计规范

- **布局**: 居中单栏，最大宽度 1200px
- **暗色主题** (默认): 背景 #0f0f0f, 控制栏 #1a1a2e, 强调色 #e94560
- **亮色主题**: 背景 #ffffff, 控制栏 #f5f5f5, 强调色 #1a73e8
- **字体**: 系统字体栈
- **圆角**: 播放器 12px, 按钮 8px
- **动画**: 控制栏显示/隐藏 0.3s 渐变, 进度条 hover 0.15s 放大
- **响应式**: 移动端控制栏简化，触摸手势支持

## 6. 组件结构

```
App
├── Header (标题 + 主题切换)
├── VideoPlayer
│   ├── Video (video元素)
│   ├── Overlay (播放按钮/加载指示器)
│   ├── Controls
│   │   ├── PlayButton
│   │   ├── ProgressBar
│   │   ├── TimeDisplay
│   │   ├── VolumeControl
│   │   ├── SpeedControl
│   │   ├── PiPButton
│   │   └── FullscreenButton
│   └── ProgressBar (含缩略图预览)
├── InputArea
│   ├── DropZone (拖拽上传)
│   └── URLInput (在线视频URL)
└── Playlist (播放列表)
```

## 7. 非功能需求

- **性能**: 视频播放器初始加载 < 1s（不含视频本身加载时间）
- **浏览器兼容**: Chrome 90+, Firefox 88+, Safari 14+, Edge 90+
- **代码质量**: TypeScript strict mode, ESLint 通过
- **无外部API依赖**: 所有功能纯前端实现

## 8. 开发规范

- 组件文件: PascalCase (`VideoPlayer.tsx`)
- 工具函数: camelCase (`formatTime.ts`)
- 样式: Tailwind CSS classes, 不写自定义CSS（除必要动画）
- 文件组织: `src/components/`, `src/hooks/`, `src/utils/`, `src/types/`
- 每个组件一个文件，不超过 200 行
- 自定义 Hook 抽离复杂逻辑 (`useVideoPlayer.ts`)

## 9. 参考项目

- Video.js (播放器UI参考)
- Plyr (控制栏交互参考)
- Apple TV+ 播放器 (极简设计参考)

## 10. 约束

- 不使用任何付费API或服务
- 不需要后端/数据库
- 纯静态站点，可直接 `npx serve dist` 部署
- 所有视频处理在浏览器端完成
